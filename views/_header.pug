header.header
    nav.nav.nav--tours
        a.nav__el(href='/') All tours

        //- using conditional statment for rendering the header for logged in user with profile phot and data , and for not logged in user with the login and signup btn
    nav.nav.nav--user
        div.header__logo
            img( src='/img/logo-white.png' alt='Natours logo')
        if user    
            a.nav__el.nav__el--logout LOG OUT
            a.nav__el(href='/myPage') 
                img.nav__user-img( src=`/img/users/${user.photo}` alt=`${user.name} photo`)
                span= user.name.split(' ')[0]
        else                
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el--cta Sign up


// header
        header.header
            nav.nav.nav--tours
                a.nav__el(href='#') All tours
            div.header__logo
            nav.nav.nav--user
                //- a.nav__el(href='#') All bookings
                //- a.nav__el(href='#') All tours
                //-     img.nav__user-img(src="img/user.jpg" alt="User photo")
                    span Jonas
                button.nav__el Log in
                button.nav__el.nav__el--cta Sign up

